<Window x:Class="AWSK.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AWSK.ViewModel"
        xmlns:c="clr-namespace:AWSK.Control"
        mc:Ignorable="d"
        Title="航空戦シミュレーター改(AWSK) Ver.2.0.0 Product by YSR" Height="250" Width="800">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <local:MainViewModel x:Key="MainViewModel"/>
    </Window.Resources>
    <StackPanel Margin="2.5,2.5,2.5,2.5">
        <!--
        <GroupBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Header="基地航空隊" Margin="2.5,2.5,2.5,2.5">
            <Grid Margin="2.5,2.5,2.5,2.5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <c:BasedAirUnitControl Grid.Column="0" BasedAirUnitUnitText="第1航空隊"/>
                <c:BasedAirUnitControl Grid.Column="1" BasedAirUnitUnitText="第2航空隊"/>
                <c:BasedAirUnitControl Grid.Column="2" BasedAirUnitUnitText="第3航空隊"/>
            </Grid>
        </GroupBox>
        -->
        <c:SelectWeaponBox x:Name="test"/>
        <Button Height="20" Width="60" Content="テスト" Command="{Binding SampleCommand}"/>
    </StackPanel>
</Window>
